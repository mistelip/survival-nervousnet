<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cpcrasher.survival">

    <uses-permission android:name="ch.ethz.coss.nervousnet.hub.BIND_PERM" />

    <application
        android:name=".mainActivity.MyQuiz"
        android:allowBackup="true"
        android:icon="@drawable/survival"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AccelerometerActivity"
            android:label="@string/title_activity_accelerometer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TsunamiActivity" />
        <activity android:name=".TerroristActivity" />
        <activity android:name=".EarthquakeActivity" />
        <activity android:name=".mainActivity.BasicNeedsActivity" />
        <activity android:name=".mainActivity.SuppNeedsActivity" />
        <activity android:name=".mainActivity.SurroundingsActivity" />
        <activity android:name=".mainActivity.ExtrasActivity"></activity>
    </application>

</manifest>